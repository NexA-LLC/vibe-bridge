# Web UI

The web UI provides visibility into runners, jobs, and logs. It is intentionally thin and relies
on the control plane API.

Current status:
- `apps/web` is still a placeholder.
- For local testing, `apps/api` serves a minimal Dev UI at `/ui`.
