# AGENTS.md

この `vibe-bridge/` は VibeBridge のワークスペース（`apps/*`, `packages/*`）です。
基本はルートの `AGENTS.md` に従い、ここでは **vibe-bridge 固有の注意点**だけを扱います。

## 原則
- 変更は最小差分（影響範囲を明確にする）。
- 不明点や前提不足は推測せず、確認する。

## Workspace
- 変更対象（`apps/*` / `packages/*`）と影響範囲を明示する。
